#!/usr/local/bin/sms
#A repl written in SMS!
#Does not require a semicolon, but
#Only allows single line commands
{
  let in = "";
  let ans = [];

  while(true){
    put("> ");
    in = input();
    ans = eval(parse(in));
    putln(toStr(ans))
  };
};
