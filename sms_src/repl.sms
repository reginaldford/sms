#!/usr/local/bin/sms
#A repl written in SMS!
#Does not require a semicolon, but
#Only allows single line commands

let in = "";
let ans = [];

while(true,(
  print("> ");
  in = input();
  ans = eval(parse(in));
  print(toStr(ans) str+ "\n")));

