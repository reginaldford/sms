{ #while statements
  x=0;
  tests=[
    [while(x<10,x=x+1), true, "basic while"],
    [while(x>0,x=x-1), true, "rewinding while"],
    [while(x<5,(x=x+1;print("repeatingThis"))), true, "repeating message"],
  ];
};
