#!./bin/sms
{
  let so=soLoad("./clib/blah.o");
  putLn(str(so));
  let sig = ffSig(@double,[@sint32]);
  putLn(str(sig));
  let f=ff(so,"fib_recursive",sig);
  putLn(str(f));
  putLn(str(f(10)));
};
